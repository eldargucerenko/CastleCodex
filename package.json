{
  "name": "castle-codex",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "tsc && vite build",
    "preview": "vite preview --host 0.0.0.0",
    "test": "vitest run"
  },
  "dependencies": {
    "phaser": "^3.90.0",
    "vite": "^6.2.0"
  },
  "devDependencies": {
    "typescript": "^5.8.2",
    "vitest": "^4.1.5"
  }
}
